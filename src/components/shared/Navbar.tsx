import React from 'react';

const Navbar = () => {
    return (
        <nav className="fixed inset-0 z-50 bg-black/95 backdrop-blur-2xl pt-24 px-6 md:hidden pointer-events-auto">
            {/* Navbar Content */}
        </nav>
    );
};

export default Navbar;