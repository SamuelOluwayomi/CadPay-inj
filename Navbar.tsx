import { motion } from 'framer-motion';

const Navbar = () => {
  return (
    <motion.div style={{ pointerEvents: 'auto', zIndex: 1000 }}>
      {/* Add your navbar content here */}
    </motion.div>
  );
};

export default Navbar;